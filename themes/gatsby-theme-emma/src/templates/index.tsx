import { css } from "theme-ui"
import React from "react"

const Projects = () => (
  <p
    css={css({
      color: `primary`,
    })}
  >
    TODO build the projects page
  </p>
)

export default Projects
